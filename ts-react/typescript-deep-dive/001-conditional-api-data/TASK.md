# Conditional Types for API Data

## Learning Objectives
- Use conditional types
- Extract payload types
- Model success and failure separately

## Task
Implement `DataOf<T>` so it extracts the `data` type from successful API results and returns `never` for failures.

Work only in the starter directory while practicing. The solution directory is a reference implementation used by the verifier.

## Expected Commands
- From this exercise: `tsc -p .`
- From the repository root: `pnpm drill:check ts-react/typescript-deep-dive/001-conditional-api-data`
- Verify the reference solution: `pnpm drill:check ts-react/typescript-deep-dive/001-conditional-api-data --solution`

## Difficulty
advanced

## Estimated Time
35 minutes

## Tags
- typescript
- conditional-types
- api
