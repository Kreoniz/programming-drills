# TypeScript Project References

## Learning Objectives
- Use tsconfig references
- Enable composite package builds
- Make build order explicit

## Task
Configure references so `apps/web` depends on `packages/ui` and both projects are composite builds.

Work only in the starter directory while practicing. The solution directory is a reference implementation used by the verifier.

## Expected Commands
- From this exercise: `node ../tests/check.mjs .`
- From the repository root: `pnpm drill:check ts-react/monorepo/002-tsconfig-references`
- Verify the reference solution: `pnpm drill:check ts-react/monorepo/002-tsconfig-references --solution`

## Difficulty
advanced

## Estimated Time
40 minutes

## Tags
- monorepo
- typescript
- project-references
